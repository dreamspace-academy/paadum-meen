---
phase: 1
plan: "02-backend-complete"
type: execute
wave: 2
depends_on:
  - "01-walking-skeleton"
files_modified:
  - backend/app/config/app.toml
  - backend/app/config/system-prompt.md
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/core/logging.py
  - backend/app/core/errors.py
  - backend/app/services/__init__.py
  - backend/app/services/openai_realtime.py
  - backend/app/services/prompt_loader.py
  - backend/app/services/metrics_service.py
  - backend/app/services/reset_service.py
  - backend/app/api/health.py
  - backend/app/api/metrics.py
  - backend/app/api/reset.py
  - backend/app/api/realtime.py
  - backend/app/models/__init__.py
  - backend/app/models/realtime_models.py
  - backend/app/models/reset_models.py
  - backend/app/models/health_models.py
  - backend/app/main.py
autonomous: true
requirements:
  - BACKEND-01
  - BACKEND-02
  - BACKEND-03
  - BACKEND-04
  - BACKEND-05
  - BACKEND-06
  - BACKEND-07
  - SAFETY-01
  - SAFETY-02
  - SAFETY-03

must_haves:
  truths:
    - "GET /health returns JSON with status=ok and all four checks true when OPENAI_API_KEY is set; returns status=degraded with HTTP 503 when key is absent"
    - "GET /metrics returns text/plain Prometheus format with paadum_realtime_sessions_created_total counter"
    - "POST /api/session/reset accepts the reset body and returns status=ok with reset.accepted=true"
    - "POST /api/realtime/session injects the system prompt from backend/app/config/system-prompt.md into the OpenAI session creation payload"
    - "No log line or HTTP response field ever contains the raw OPENAI_API_KEY or the ephemeral client secret value"
  artifacts:
    - path: "backend/app/config/app.toml"
      provides: "Non-secret configuration: model, voice, timeouts, logging, metrics"
      contains: "[realtime]"
    - path: "backend/app/config/system-prompt.md"
      provides: "Server-owned mascot system prompt injected at session creation"
    - path: "backend/app/core/config.py"
      provides: "Settings class (pydantic-settings) reading from .env and app.toml"
      exports: ["Settings", "get_settings"]
    - path: "backend/app/api/health.py"
      provides: "GET /health endpoint"
      exports: ["router"]
    - path: "backend/app/api/metrics.py"
      provides: "GET /metrics Prometheus text endpoint"
      exports: ["router"]
    - path: "backend/app/api/reset.py"
      provides: "POST /api/session/reset endpoint"
      exports: ["router"]
    - path: "backend/app/services/metrics_service.py"
      provides: "Prometheus counter registry and increment functions"
      exports: ["increment_sessions_created", "increment_resets", "increment_errors"]
  key_links:
    - from: "backend/app/api/realtime.py"
      to: "backend/app/services/prompt_loader.py"
      via: "load_system_prompt() called before OpenAI session creation"
      pattern: "load_system_prompt"
    - from: "backend/app/api/realtime.py"
      to: "backend/app/services/metrics_service.py"
      via: "increment_sessions_created() after successful session"
      pattern: "increment_sessions_created"
    - from: "backend/app/api/health.py"
      to: "backend/app/core/config.py"
      via: "get_settings() to check openai_api_key is configured"
      pattern: "get_settings"
---

<objective>
Complete the backend by adding all remaining endpoints (/health, /metrics, /api/session/reset), TOML configuration, Prometheus metrics, structured JSON logging, system prompt injection, and proper Pydantic settings. This plan converts the skeleton backend into a production-quality thin backend.

Purpose: The skeleton proved the session endpoint works. This plan hardens it — adding observability, configuration, safety controls (system prompt), and the endpoints needed for admin reset and health monitoring.

Output: A fully wired FastAPI backend matching the complete API contract from docs/api-contract.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/system-architecture.md
@docs/api-contract.md
@tmp/system-pompt.md
@.planning/phases/01-working-voice/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configuration, logging, system prompt, and Prometheus metrics</name>

  <read_first>
    docs/system-architecture.md (§8.1 folder structure, §9.1 secret config, §9.2 TOML config shape, §9.3 system prompt config, §8.7 metrics list, §12.1 logging fields, §12.3 sensitive logging rules)
    docs/api-contract.md (§10.4 recommended metrics, §17 logging contract)
    tmp/system-pompt.md (full content — copy verbatim to backend/app/config/system-prompt.md)
  </read_first>

  <files>
    backend/app/config/app.toml
    backend/app/config/system-prompt.md
    backend/app/core/__init__.py
    backend/app/core/config.py
    backend/app/core/logging.py
    backend/app/core/errors.py
    backend/app/services/__init__.py
    backend/app/services/prompt_loader.py
    backend/app/services/metrics_service.py
  </files>

  <action>
Install additional backend dependencies: `uv add pydantic-settings prometheus-client tomllib` (tomllib is stdlib in Python 3.11+; include as optional fallback import: `try: import tomllib except ImportError: import tomli as tomllib` — but since we require Python 3.12, use stdlib tomllib only).

`backend/app/config/app.toml` — create with this exact structure:
```toml
[app]
name = "Paadum Meen"
version = "0.1.0"
environment = "development"

[realtime]
model = "gpt-4o-realtime-preview-2024-12-17"
voice = "alloy"
subtitles_enabled = true
session_refresh_margin_seconds = 60

[interaction]
audio_input_mode = "hold_to_talk"
idle_timeout_seconds = 30

[ui]
default_language = "en"
supported_languages = ["en", "ta", "si"]

[logging]
level = "INFO"
structured = true

[metrics]
enabled = true
```

`backend/app/config/system-prompt.md` — copy the full content verbatim from `tmp/system-pompt.md` (note the typo in the source filename — the destination file should be named `system-prompt.md` with the correct spelling).

`backend/app/core/__init__.py` — empty.

`backend/app/core/config.py`:
- Import `pydantic_settings.BaseSettings`, `pydantic.Field`, `tomllib`, `functools.lru_cache`, `pathlib.Path`.
- Define `class Settings(BaseSettings)`:
  - `openai_api_key: str = Field(default="", env="OPENAI_API_KEY")`
  - `backend_host: str = Field(default="127.0.0.1", env="BACKEND_HOST")`
  - `backend_port: int = Field(default=8000, env="BACKEND_PORT")`
  - Load TOML inside `model_post_init`: read `backend/app/config/app.toml` using `tomllib.load()` and store as `self._toml: dict`.
  - Expose helpers: `@property def realtime_model(self) -> str`, `@property def realtime_voice(self) -> str`, `@property def idle_timeout_seconds(self) -> int`, `@property def subtitles_enabled(self) -> bool`, `@property def metrics_enabled(self) -> bool`.
  - `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")`.
- Define `@lru_cache def get_settings() -> Settings: return Settings()`.
- CRITICAL: `openai_api_key` must NEVER be returned from any property as a plain string in a context where it could be serialized to a response. It is only used inside `services/openai_realtime.py`.

`backend/app/core/logging.py`:
- Configure structured JSON logging using the stdlib `logging` module.
- `setup_logging(level: str = "INFO") -> None`: calls `logging.basicConfig(level=..., format='%(message)s')` and installs a custom `logging.Formatter` that formats each record as JSON:
  `{"time": <ISO8601>, "level": <levelname>, "event": <message>, **extra_fields}`.
- Export `get_logger(name: str) -> logging.Logger` as a convenience wrapper.
- CRITICAL: The formatter must NEVER include fields named `openai_api_key`, `client_secret`, `clientSecret`, or `authorization`. If any of these appear in the `extra` dict, replace the value with `"[REDACTED]"`.

`backend/app/core/errors.py`:
- Define `class PaadumError(Exception)` with fields `code: str`, `message: str`, `http_status: int = 500`.
- Define constants: `OPENAI_API_KEY_MISSING = "OPENAI_API_KEY_MISSING"`, `OPENAI_SESSION_CREATE_FAILED = "OPENAI_SESSION_CREATE_FAILED"`, `OPENAI_UNREACHABLE = "OPENAI_UNREACHABLE"`, `INVALID_REQUEST = "INVALID_REQUEST"`, `INTERNAL_ERROR = "INTERNAL_ERROR"`, `RESET_LOG_FAILED = "RESET_LOG_FAILED"`.

`backend/app/services/__init__.py` — empty.

`backend/app/services/prompt_loader.py`:
- `load_system_prompt() -> str`: reads `backend/app/config/system-prompt.md` using `pathlib.Path(__file__).parent.parent / "config" / "system-prompt.md"`. Returns full text content. Raises `PaadumError(INTERNAL_ERROR)` if file not found.
- Cache the result with `functools.lru_cache` (system prompt is static at runtime).

`backend/app/services/metrics_service.py`:
- Import `prometheus_client` (`Counter`).
- Define counters:
  - `sessions_created = Counter("paadum_realtime_sessions_created_total", "Total realtime sessions created", ["model"])`
  - `session_errors = Counter("paadum_realtime_session_create_errors_total", "Session creation errors", ["code"])`
  - `resets_total = Counter("paadum_session_resets_total", "Session resets", ["reason"])`
  - `responses_done = Counter("paadum_ai_response_done_total", "Completed AI responses")`
  - `response_errors = Counter("paadum_ai_response_errors_total", "AI response errors", ["code"])`
- Export increment functions:
  - `def increment_sessions_created(model: str) -> None`
  - `def increment_session_error(code: str) -> None`
  - `def increment_reset(reason: str) -> None`
  - `def increment_response_done() -> None`
  - `def increment_response_error(code: str) -> None`
  </action>

  <verify>
    <automated>
      cd /home/gova/workbench/dst/paadum-meen/backend && uv run python -c "from app.core.config import get_settings; from app.services.prompt_loader import load_system_prompt; from app.services.metrics_service import increment_sessions_created; s=get_settings(); print('settings ok'); print('prompt ok:', len(load_system_prompt()) > 100)"
    </automated>
  </verify>

  <done>
    - `app/config/app.toml` exists and contains `[realtime]` section with model and voice
    - `app/config/system-prompt.md` exists and contains "Paadum Meen" and "DreamSpace" (copied from tmp/system-pompt.md)
    - `get_settings()` instantiates without error and `get_settings().realtime_model` returns the model string from TOML
    - `load_system_prompt()` returns a non-empty string
    - `grep -r "openai_api_key" backend/app/core/logging.py` shows only the REDACTED filtering logic, not any value exposure
  </done>
</task>

<task type="auto">
  <name>Task 2: /health, /metrics, /api/session/reset endpoints + wire everything into main.py</name>

  <read_first>
    docs/system-architecture.md (§8.2 endpoint list, §8.5 reset endpoint flow, §8.6 health endpoint, §8.7 metrics endpoint)
    docs/api-contract.md (§7 reset endpoint full spec, §8 health endpoint spec with degraded 503, §10 metrics spec)
    backend/app/core/config.py (just written — check Settings properties)
    backend/app/services/metrics_service.py (just written — check counter names)
  </read_first>

  <files>
    backend/app/models/__init__.py
    backend/app/models/realtime_models.py
    backend/app/models/reset_models.py
    backend/app/models/health_models.py
    backend/app/api/health.py
    backend/app/api/metrics.py
    backend/app/api/reset.py
    backend/app/api/realtime.py
    backend/app/services/reset_service.py
    backend/app/services/openai_realtime.py
    backend/app/main.py
  </files>

  <action>
`backend/app/models/__init__.py` — empty.

`backend/app/models/realtime_models.py`:
- `SessionCreateRequest(BaseModel)`: `clientSessionId: str`, `reason: Literal["app_start", "manual_admin", "idle_timeout", "error_recovery", "connection_recovery"]`.
- `SessionData(BaseModel)`: `id: str`, `clientSecret: str`, `expiresAt: str | None`, `model: str`, `voice: str`.
- `SessionConfig(BaseModel)`: `idleTimeoutSeconds: int`, `subtitlesEnabled: bool`, `audioInputMode: str`.
- `SessionCreateResponse(BaseModel)`: `status: str`, `session: SessionData`, `config: SessionConfig`.

`backend/app/models/reset_models.py`:
- `ResetRequest(BaseModel)`: `clientSessionId: str`, `reason: Literal["manual_admin", "idle_timeout", "error_recovery", "app_start", "connection_recovery"]`, `previousState: str | None = None`, `openaiSessionId: str | None = None`.
- `ResetResult(BaseModel)`: `accepted: bool`, `serverReceivedAt: str`.
- `ResetResponse(BaseModel)`: `status: str`, `reset: ResetResult`.

`backend/app/models/health_models.py`:
- `HealthChecks(BaseModel)`: `processAlive: bool`, `openaiApiKeyConfigured: bool`, `openaiRealtimeConfigured: bool`, `metricsEnabled: bool`.
- `HealthResponse(BaseModel)`: `status: str`, `service: str`, `version: str`, `time: str`, `checks: HealthChecks`, `error: dict | None = None`.

`backend/app/services/reset_service.py`:
- `async def log_reset(request: ResetRequest, logger: logging.Logger) -> None`: logs structured JSON with fields `event="session_reset"`, `clientSessionId`, `reason`, `previousState`, `openaiSessionId`. Does NOT log API key or client secret.
- Calls `metrics_service.increment_reset(request.reason)`.

`backend/app/services/openai_realtime.py`:
- Move the OpenAI HTTP call logic OUT of the route handler (it was inline in skeleton). Expose: `async def create_realtime_session(client_session_id: str, settings: Settings) -> dict`.
- Uses `httpx.AsyncClient`, reads `settings.openai_api_key`, `settings.realtime_model`, `settings.realtime_voice`.
- Loads system prompt via `prompt_loader.load_system_prompt()` and includes it in the OpenAI POST body as `"instructions": prompt_text`.
- Body to OpenAI: `{"model": settings.realtime_model, "voice": settings.realtime_voice, "instructions": system_prompt}`.
- CRITICAL: The `settings.openai_api_key` value is used ONLY as the Authorization header value. It must not be returned, logged, or included in any other field.
- On success: call `increment_sessions_created(model=settings.realtime_model)`.
- On error: call `increment_session_error(code=...)`.
- Returns the full OpenAI response dict.

`backend/app/api/realtime.py` — REFACTOR from skeleton version:
- Remove inline httpx logic; delegate to `openai_realtime.create_realtime_session()`.
- Import models from `app.models.realtime_models`.
- Route handler: `async def create_realtime_session(body: SessionCreateRequest) -> JSONResponse`.
- On success: return `SessionCreateResponse` mapping from OpenAI response per api-contract.md §6.4.
- On error: return structured error per api-contract.md §6.6.

`backend/app/api/health.py`:
- `router = APIRouter()`.
- `GET /health`: checks `bool(get_settings().openai_api_key)` for `openaiApiKeyConfigured`. Sets `openaiRealtimeConfigured = openaiApiKeyConfigured` (same key gates realtime). Checks `get_settings().metrics_enabled` for `metricsEnabled`. `processAlive` is always `True`.
- Returns HTTP 200 with `status=ok` when all checks pass.
- Returns HTTP 503 with `status=degraded` and error block `{"code": "OPENAI_API_KEY_MISSING", "message": "OpenAI API key is not configured."}` when `openaiApiKeyConfigured=False`.
- Response body matches api-contract.md §8.2 and §8.3 exactly.
- Include `time: datetime.utcnow().isoformat() + "Z"` in response.

`backend/app/api/metrics.py`:
- `router = APIRouter()`.
- `GET /metrics`: return `prometheus_client.generate_latest()` with `Content-Type: text/plain; version=0.0.4`.
- Use `Response(content=generate_latest(), media_type=CONTENT_TYPE_LATEST)` from `prometheus_client`.

`backend/app/api/reset.py`:
- `router = APIRouter()`.
- `POST /api/session/reset`: accepts `ResetRequest`, calls `reset_service.log_reset(request, logger)`, returns `ResetResponse` with `accepted=True` and `serverReceivedAt=datetime.utcnow().isoformat()+"Z"`.
- If `log_reset` raises, still return HTTP 200 with `accepted=True` — frontend resets locally regardless (per api-contract.md §7.6). Log the error server-side.

`backend/app/main.py` — REPLACE skeleton version:
- `load_dotenv()` at top.
- `setup_logging(get_settings().toml_logging_level)` on startup.
- Mount routers:
  - `app.include_router(realtime.router, prefix="/api")`
  - `app.include_router(reset.router, prefix="/api")`  (note: reset route path is `/api/session/reset` so prefix the router at `/api` and the route itself as `/session/reset`)
  - `app.include_router(health.router)` (no prefix — route is `/health`)
  - `app.include_router(metrics.router)` (no prefix — route is `/metrics`)
- CORS: same as skeleton (`allow_origins=["http://localhost:5173"]`).
- Add startup event: log `{"event": "backend_started", "version": "0.1.0"}`.
  </action>

  <verify>
    <automated>
      cd /home/gova/workbench/dst/paadum-meen/backend && uv run python -c "from app.main import app; routes=[r.path for r in app.routes]; print(routes); assert '/health' in routes or any('/health' in str(r) for r in app.routes)"
    </automated>
  </verify>

  <done>
    - `curl -s http://localhost:8000/health | python3 -m json.tool` returns JSON with `checks.processAlive=true` (requires running server)
    - `curl -s http://localhost:8000/metrics | grep paadum_realtime_sessions_created_total` returns the counter line
    - `curl -s -X POST http://localhost:8000/api/session/reset -H 'Content-Type: application/json' -d '{"clientSessionId":"pm_test","reason":"manual_admin"}' | python3 -m json.tool` returns `{"status":"ok","reset":{"accepted":true,...}}`
    - `curl -s -X POST http://localhost:8000/api/realtime/session -H 'Content-Type: application/json' -d '{"clientSessionId":"pm_test","reason":"app_start"}' | grep -c "clientSecret"` returns 1 (secret present in response)
    - `curl -s -X POST http://localhost:8000/api/realtime/session ... | grep -c "OPENAI_API_KEY"` returns 0 (key never in response)
    - Backend logs show `{"event": "backend_started"}` on startup; no log line contains `sk-` or `ek_` prefix strings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → /api/session/reset | Admin-triggered POST — untrusted body crosses here |
| Backend → OpenAI | httpx POST using OPENAI_API_KEY to create session; system prompt injected here |
| /health endpoint | Public endpoint — must not leak configuration secrets |
| /metrics endpoint | Public endpoint — must not leak session content, API keys, or visitor data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | backend/app/core/logging.py | mitigate | Custom formatter redacts any field named openai_api_key, client_secret, clientSecret, authorization with [REDACTED] |
| T-02-02 | Information Disclosure | GET /health | mitigate | Health response contains only boolean check results — no env var values, no key fragments |
| T-02-03 | Information Disclosure | GET /metrics | accept | Prometheus metrics expose counters only (counts, not values); no visitor data or secrets in metric labels |
| T-02-04 | Tampering | POST /api/session/reset | accept | localhost-only MVP; reset endpoint only logs and increments a counter — no state mutation on backend |
| T-02-05 | Information Disclosure | backend/app/services/openai_realtime.py | mitigate | OPENAI_API_KEY used only as Authorization header value; function returns OpenAI response dict, not the key |
| T-02-06 | Repudiation | POST /api/session/reset | mitigate | Reset events logged with clientSessionId, reason, previousState, openaiSessionId for audit trail |
| T-02-SC | Tampering | uv add prometheus-client pydantic-settings | mitigate | Both are established PyPI packages; verify via pypi.org/project/prometheus-client and pypi.org/project/pydantic-settings before install |
</threat_model>

<verification>
- All four endpoints respond: GET /health (200 or 503), GET /metrics (200 text/plain), POST /api/realtime/session (200 or 500), POST /api/session/reset (200)
- System prompt from backend/app/config/system-prompt.md contains "Paadum Meen" and is injected as `instructions` field in OpenAI session creation body (verify by adding a temporary debug log of the OpenAI request body keys — not values)
- `grep -r "openai_api_key\|OPENAI_API_KEY" backend/app/ | grep -v "os.environ\|env=\|Field\|core/errors\|\.env"` returns no lines that assign or return the key value
- `grep -r "clientSecret\|client_secret" backend/app/ | grep "log\|print"` returns no results (secret never logged)
</verification>

<success_criteria>
- GET /health returns the exact JSON shape from api-contract.md §8.2
- GET /metrics returns Prometheus text format with at least paadum_realtime_sessions_created_total defined
- POST /api/session/reset returns {"status":"ok","reset":{"accepted":true,...}} for any valid body
- POST /api/realtime/session injects the full system prompt from backend/app/config/system-prompt.md into the OpenAI API call
- No log line, HTTP response, or Python object ever contains the raw OPENAI_API_KEY value
</success_criteria>

<output>
Create `.planning/phases/01-working-voice/01-02-SUMMARY.md` when done.
</output>
